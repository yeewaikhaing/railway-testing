import { Logger } from '../../core';
/**
 * Create the new directory, if necessary, that will receive the components.
 * @param relativeDestinationPath
 * @param fullDestinationPath
 * @param logger
 */
export declare function createDirectoryIfNecessary(relativeDestinationPath: string, fullDestinationPath: string, logger?: Logger | typeof console): void;
