declare type Options = {
    run: boolean;
    revert: boolean;
    show: boolean;
    tenant_codes: string | null;
};
/**
 * Run the migrations using the medusa-config.js config.
 * @param run
 * @param revert
 * @param show
 * @param tenant_codes
 */
export declare function migrate({ run, revert, show, tenant_codes }: Options): Promise<void>;
export {};
