/**
 * Run the migrations using the medusa-config.js config.
 * @param run
 * @param revert
 * @param show
 */
export declare function migrate({ run, revert, show }: {
    run: any;
    revert: any;
    show: any;
}): Promise<void>;
