import { Logger } from './logger';
/**
 * @Internal
 * Load packages at run time.
 * @param logger
 * @param packages The packages descriptors that must be installed
 */
export declare function loadPackages(logger: Logger, packages: {
    name: string;
    version: string;
    isDevDependency?: boolean;
}[]): Promise<void>;
/**
 * @Internal
 * Return the install command from the user package manager.
 */
export declare function getPackageManagerCommand(): Promise<string | never>;
