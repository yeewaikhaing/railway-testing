import { MedusaDynamicModule, ModuleInjectionOptions } from '../../core';
import { ConfigModule } from './types';
export declare class TenantModule implements MedusaDynamicModule {
    forRoot(configModule: ConfigModule): Promise<ModuleInjectionOptions>;
}
