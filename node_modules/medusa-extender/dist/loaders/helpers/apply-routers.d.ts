import { Router } from 'express';
import { GetInjectableOptions } from '../../core';
/**
 * @internal
 * Apply all routes on the app that must be applied before the @medusajs authentication
 * @param app
 * @param routesOptions
 */
export declare function applyBeforeAuthRouters(app: Router, routesOptions: GetInjectableOptions<'router'>): void;
/**
 * @internal
 * Apply all routes on the app that must be applied after the @medusajs authentication
 * @param app
 * @param routesOptions
 */
export declare function applyAfterAuthRouters(app: Router, routesOptions: GetInjectableOptions<'router'>): void;
