import { Router } from 'express';
import { GetInjectableOptions } from '../../core';
/**
 * @internal
 * Apply all middlewares on the app that must be applied before the @medusajs authentication
 * @param app
 * @param middlewares
 */
export declare function applyBeforeAuthMiddleware(app: Router, middlewares: GetInjectableOptions<'middleware'>): void;
/**
 * @internal
 * Apply all middlewares on the app that must be applied after the @medusajs authentication
 * @param app
 * @param middlewares
 */
export declare function applyAfterAuthMiddleware(app: Router, middlewares: GetInjectableOptions<'middleware'>): void;
